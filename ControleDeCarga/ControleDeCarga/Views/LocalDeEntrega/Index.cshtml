@model IEnumerable<ControleDeCarga.Models.LocalDeEntrega>

@{
    ViewBag.Title = "Locais de Entrega";
}
@section Scripts {
     <script type="text/javascript">
        StyleGridButtons();        
     </script>
}
<hr/>
<div class="container">
<p>
    @Html.ActionLink("Criar novo", "Create")
</p>
    <div class="col-lg-5">
        <table class="table table-striped">
            <tr>
                <th></th>
                <th>
                    @Html.DisplayNameFor(model => model.Uf.Sigla)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Cidade)
                </th>

            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { data_edit = true })
                        @Html.ActionLink("Detalhes", "Details", new { id = item.Id }, new { data_detail = true })
                        @Html.ActionLink("Excluir", "Delete", new { id = item.Id }, new { data_delete = true })
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Uf.Sigla)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cidade)
                    </td>
                </tr>
            }

        </table>
    </div>
</div>